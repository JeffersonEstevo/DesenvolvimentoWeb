No diretório público nesse caso aqui no linux
os arquivos necessário para rodar o twitter clone 
ficaram da seguinte forma:

na pasta: (var/www/html) = diretŕorio público
contém os arquivos: index.php / css(pasta) / twitter_clone(pasta) / .htaccess

a pasta twitter_clone contém o coração da aplicação

OBS.: A aplicação só funcionou com os diretórios no diretório público
em var/www/html
